
export default function Dot({type}) {

    return (
        <div></div>
        // <div className={`absolute top-${dot.top} left-${dot.left} w-${dot.size} h-${dot.size} bg-${dot.color}-300 rounded-full mix-blend-multiply filter blur-md opacity-90`}></div>
    )
}